# MLflow tracking server — values. K8s port of the docker-compose `mlflow` service.
# Metadata in Postgres (the db component below); artifacts in MinIO (its S3 creds
# must match the MinIO root user/password; the `mlflow` bucket is created by the
# MinIO subchart).
enabled: true

replicaCount: 1

image:
  repository: ghcr.io/gridatek/mlflow    # mlflow + boto3 + psycopg2 (publish-images.yml)
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5000

# Object storage for run artifacts.
s3:
  endpoint: http://minio:9000
  region: us-east-1
  bucket: mlflow
  accessKey: admin
  secretKey: password

# Postgres backend store (run/experiment metadata).
db:
  image:
    repository: postgres
    tag: "15"
    pullPolicy: IfNotPresent
  user: mlflow
  password: mlflow
  name: mlflow
  service:
    port: 5432
  persistence:
    enabled: true
    size: 5Gi
    # storageClass: ""
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi

startup:
  periodSeconds: 10
  failureThreshold: 30

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi
