package com.gridatek.odp.console;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Wiring smoke test: the context loads and all beans resolve (no network calls at startup). */
@SpringBootTest
class ConsoleApplicationTests {

    @Test
    void contextLoads() {
    }
}
